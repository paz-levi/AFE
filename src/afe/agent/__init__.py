"""
Agent package — the demo agent (harness) and its mock tools.

This is the "brain" that AFE sits in front of: an LLM-driven tool-calling loop, kept
deliberately separate from AFE's own decision engine (see docs/concept.md §2.3).
"""
