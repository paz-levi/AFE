"""
AFE — Alpha-Flow Engine.

Package root for AFE: a mandatory chokepoint between an AI agent and every classified
resource it tries to reach, that validates Just-In-Time whether the agent's current
request still matches the task it was dispatched for (its signed Baseline), rather than
only checking permissions once at creation time. See docs/concept.md for the full
architecture.
"""
